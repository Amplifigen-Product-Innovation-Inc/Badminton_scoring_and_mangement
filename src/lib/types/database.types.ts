/**
 * PLACEHOLDER — regenerate this file once the schema (supabase/migrations/0001_init_schema.sql)
 * has been applied to a real Supabase project:
 *
 *   supabase gen types typescript --project-id <ref> > src/lib/types/database.types.ts
 *
 * or, against a local dev stack:
 *
 *   supabase gen types typescript --local > src/lib/types/database.types.ts
 *
 * Left loose (`any`-shaped) for now so the Supabase client factories in
 * src/lib/supabase/*.ts compile before the schema is generated. Every table/enum name here
 * MUST match supabase/migrations/0001_init_schema.sql exactly once regenerated — do not
 * hand-edit this file after that point.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
